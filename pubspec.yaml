name: ttc
description: "The Trading Console"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  google_sign_in: ^7.2.0
  flutter_facebook_auth: ^7.0.1
  go_router: ^17.0.0
  firebase_app_check: ^0.4.1+2
  font_awesome_flutter: ^10.12.0
  google_fonts: ^6.3.2
  cloud_firestore: ^6.1.0
  dart_nats: ^0.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
